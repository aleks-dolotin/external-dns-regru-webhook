apiVersion: v1
kind: Secret
metadata:
  name: reg-credentials
  namespace: reg-adapter
type: Opaque
data:
  # Base64-encoded examples; replace with kubectl create secret or sealed secret
  username: "" # e.g. base64(username)
  password: "" # e.g. base64(password)

